extends layouts/default

block classBlock
    - bodyClasses.push('home')
    - breadcrumb = 'Home'
    - pageId = 'home'

block content
    .r-row
        .r-wrap
            .r-col
                .logo 
                    span
                        | Revocss
        .r-wrap
            .r-col
                .title
                    p Lightweight css framework
